<div class='row'>
  <div class='col-xs-6'>
    <%= simple_nested_form_for [:manage, @cutaway] do |f|  %>
      <%= f.input :file %>
      <%= render :partial => 'nested_slide_form', locals: { f: f } %>
      <%= f.button :submit  %>
    <% end %>
  </div>
</div>